{ 
  # Código de header (opcional)
  import myToken
}
let DIGIT = [0-9]
let LETTER = [A-Za-z]
rule gettoken =
    [' ' '\t'] { return "skip" }
    | ['\n'] { return "EOL" }
    | DIGIT+ { return int(lexeme) }
    | '+' { return "PLUS" }
    | '-' { return "MINUS" }
    | eof { raise Exception("Fin de buffer") }
{ 
  # Código de trailer (opcional)
}
